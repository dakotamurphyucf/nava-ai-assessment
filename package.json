{
  "name": "nava-ai-assessment-server",
  "version": "0.0.1",
  "description": "MCP server for blockchain data",
  "type": "module",
  "bin": {
    "mcp-server-blockchain": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && shx chmod +x dist/*.js",
    "prepare": "npm run build",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "0.5.0",
    "glob": "^10.3.10",
    "zod-to-json-schema": "^3.23.5",
    "jsbi": "^4.3.0",
    "viem": "^2.22.21"
  },
  "devDependencies": {
    "@types/node": "^22",
    "shx": "^0.3.4",
    "typescript": "^5.3.3"
  }
}
